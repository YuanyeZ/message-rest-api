package com.qlik.interview.service.validators;

public interface PalindromeValidator {
    /**
     * Function that check the string is palindrome or not
     * @param content the message that needs to be validated
     * @return whether the input msg is a palindrome or not
     */
    boolean isPalindrome(String content);
}
